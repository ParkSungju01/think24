# 고민 관리 (Worry Management)

사용자가 충동구매 욕구를 느낀 순간 상품을 "고민"으로 등록하고, 24시간 타이머 동안 지켜본 뒤 실제로 포기했는지/구매했는지를 기록해 절약을 유도하는 이 프로젝트의 핵심 도메인.

## Language

**고민 (Worry)**:
사용자가 충동구매 욕구를 느낀 상품에 대해 등록하는, 24시간 타이머가 붙은 의사결정 단위. 상품명·가격·카테고리와 함께 저장된다.
_Avoid_: 상품, 아이템, 항목

**고민 상태 (Worry Status)**:
고민이 가질 수 있는 세 값 — `ongoing`(타이머 진행 중, 아직 결과 없음) / `abandoned`(포기함 → 절약으로 집계) / `purchased`(구매함). 타이머가 끝났다고 자동으로 바뀌지 않으며, [[확정]] 행위를 통해서만 `ongoing`에서 벗어난다.

**판정 (AI Verdict)**:
타이머를 시작하기 **전**, AI가 등록된 상품에 대해 "필요한 소비인지/충동적인 소비인지"를 평가해 사용자에게 제시하는 추천 결과(`necessary`/`unnecessary`). 아직 아무 결정도 내려지지 않은 시점의 참고 의견일 뿐, 실제 구매 여부와는 무관하다. [[확정]]과 혼동하지 말 것.

**확정 (Outcome Confirmation)**:
타이머 종료 **후**, 사용자가 실제로 포기했는지 구매했는지를 기록해 고민 상태를 `ongoing`에서 `abandoned`/`purchased`로 전환하는 행위. "고민 목록" 화면의 [[결정 대기]] 카드 → "최종 결정하기" 버튼이 이 트리거 UI다. 다만 이 액션은 현재 프론트엔드 로컬 상태로만 반영되고 DB에는 쓰지 않는다(근거는 ADR 예정) — 그래서 마감이 지난 고민은 새로고침하면 여전히 코드상 `ongoing`으로 남는다.
_Avoid_: 판정(위 AI Verdict와 다른 개념이므로 이 이름으로 부르지 않는다)

**결정 대기 (Decision Pending)**:
고민 상태가 `ongoing`이면서 `deadlineAt`이 지났지만 아직 [[확정]]되지 않은 상태를 가리키는 파생(view-level) 상태 — 저장된 값이 아니라 `status === 'ongoing' && now >= deadlineAt` 조건으로 매번 계산한다. "고민 목록" 화면에서만 필터/강조 표시되며, [[확정]]("최종 결정하기") 전에는 삭제할 수 없다.
_Avoid_: 마감됨, 만료됨

**일시정지 (Paused)**:
"고민 목록" 화면에서 사용자가 진행 중인(`ongoing`, 아직 [[결정 대기]]가 아닌) 고민의 24시간 타이머를 임시로 멈추는 상태. [[결정 대기]]가 된 고민은 일시정지할 수 없다. 현재는 프론트엔드 로컬 상태로만 존재하며 DB 스키마 변경 없이 구현되어, 새로고침하면 초기화된다(근거는 ADR 예정).
_Avoid_: 정지, 멈춤(모두 "일시정지"로 통일)

**절약 금액 (Savings Amount)**:
특정 기간 동안 `decidedAt`(확정된 시각) 기준으로 `abandoned` 상태가 된 고민들의 `price` 합산액. 고민을 등록한 시각(`createdAt`)이 아니라 확정된 시각이 기준이다.
_Avoid_: 절감액, 세이빙

**소비기록 (Spending Record dashboard)**:
확정된 고민들의 이력을 기간(이번 달/최근 3개월/최근 1년/전체)별로 집계해 보여주는 조회 전용 대시보드 화면. 개별 거래 목록이나 수동 입력 기능은 포함하지 않는다. 실제 `worries` 데이터를 조회하지 않고, 전 사용자에게 동일한 정적 mock 데이터를 보여준다(`docs/adr/0003-spending-record-fully-static-mock.md`).
_Avoid_: 가계부, 소비 내역 리스트
